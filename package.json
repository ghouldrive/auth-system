{
  "name": "auth-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd auth-project && npm run dev",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "concurrently": "^9.0.0"
  }
}